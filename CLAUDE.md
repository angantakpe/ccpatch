# See `AGENTS.md` and `RULES.md`

Follow `AGENTS.md` for repo layout and `RULES.md` for the repo-specific agent
rules.

`WORKFLOW.md` is live configuration for the Symphony autonomous orchestrator, not
documentation — leave it in place (see `AGENTS.md`).

For a fast repo snapshot, run:

```bash
bash scripts/context.sh
```
